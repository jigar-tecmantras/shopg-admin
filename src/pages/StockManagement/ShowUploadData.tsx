import React from 'react';
import {Container} from 'react-bootstrap';

const ShowUploadData = (): JSX.Element => {
  return (
    <div className="page-content">
      <Container fluid={true}></Container>
    </div>
  );
};

export default ShowUploadData;
